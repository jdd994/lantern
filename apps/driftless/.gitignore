node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
*.log

# Cloudflare / wrangler local state + secrets
.wrangler/
.dev.vars

# Database backups — contain account metadata, password hashes, and feedback.
# Never commit; keep them somewhere safe locally / offline.
backups/

# vite-plugin-pwa dev-mode service-worker artifacts
dev-dist/
