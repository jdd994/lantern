name = "manifest-server"
main = "src/index.ts"
compatibility_date = "2024-12-01"

[[d1_databases]]
binding = "DB"
database_name = "manifest"
database_id = "590a0823-b4ca-4c62-a062-073a3498c23e"

[vars]
# Comma-separated allowed origins. Add the real domain if one is ever bought;
# empty string reflects * (dev only).
ALLOWED_ORIGIN = "https://manifest-8rk.pages.dev"
