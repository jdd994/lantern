{
  "name": "hearth-server",
  "private": true,
  "type": "module",
  "description": "Hearth sync server — stores opaque ciphertext only. Ported from Driftless.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute hearth --remote --file=schema.sql",
    "db:init:local": "wrangler d1 execute hearth --local --file=schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.5.3",
    "wrangler": "^3.95.0"
  }
}
