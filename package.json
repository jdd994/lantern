{
  "name": "lantern",
  "private": true,
  "version": "0.0.0",
  "description": "Local-first, end-to-end-encrypted apps that share one core. Driftless, Ballast, Hearth — one soul, three flavors.",
  "workspaces": [
    "packages/*",
    "apps/*",
    "apps/*/server"
  ],
  "scripts": {
    "vibe-relay": "npm run start -w @lantern/vibe-relay"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
